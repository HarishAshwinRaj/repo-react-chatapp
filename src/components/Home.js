import React from "react";
import ListContact from "./ListContact";

const Home = () => {
  return (
    <div style={{ flexDirection: "columns" }}>
      <ListContact />
    </div>
  );
};

export default Home;
